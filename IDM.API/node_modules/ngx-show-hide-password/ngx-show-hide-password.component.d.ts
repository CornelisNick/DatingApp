import { ElementRef, OnInit, Renderer2 } from '@angular/core';
/**
 * Add a split input button to password or text input. Toggles input type between "text" and "password".
 *
 * @example
 * <show-hide-password size="sm|lg">
 * <input type="password" name=... />
 * </show-hide-password>
 */
export declare class ShowHidePasswordComponent implements OnInit {
    private elem;
    private renderer;
    /**
     * Bootstrap 4 style, default is secondary
     */
    btnStyle: 'primary' | 'secondary' | 'success' | 'danger' | 'warning' | 'info';
    /**
     * Button outline style, default is true
     */
    btnOutline: boolean;
    /**
     * can be 'sm' = small, 'lg' = large or empty = default
     */
    size: 'sm' | 'lg' | '';
    /**
     * can be 'fontawesome', 'entypo' or empty for checkbox
     */
    icon: 'fontawesome' | 'entypo' | '';
    /**
     * the shielded Input element
     */
    input: any;
    /**
     * current state
     */
    isHidden: boolean;
    constructor(elem: ElementRef, renderer: Renderer2);
    /**
     * init component
     */
    ngOnInit(): void;
    /**
     * toggles type of input (text|password)
     * @param $event not used
     */
    toggleShow($event: any): void;
}
